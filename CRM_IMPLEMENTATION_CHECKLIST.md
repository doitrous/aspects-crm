# CRM Implementation Checklist

Production rule: do not mark a feature complete because UI exists. A feature is complete only when the real action works, writes persist, refresh still shows the result, deployed-production code path is supported, permissions are enforced, errors are handled, related counters/views update, and it is tested.

## Financial Phase Verification — 2026-07-11

| Requirement | Status | Evidence | Files | Database objects | Test performed | Result | Remaining issue |
|---|---|---|---|---|---|---|---|
| One financial source of truth | PASS | Lead Payments, Financial Dashboard, settings, imports, and audit flows continue to use canonical financial tables and shared finance engine; no duplicate final finance dataset was added. | `lib/data/financials.ts`, `lib/data/financialDashboard.ts`, `lib/financial/*`, `app/(crm)/financial/page.tsx` | `crm_lead_financials`, `crm_financial_transactions`, `crm_discount_rules`, `crm_discount_approvals`, `crm_lead_consumables`, `crm_lead_doctor_compensation`, `crm_doctor_funded_payments`, `crm_external_costs`, `crm_financial_audit_log` | Code inspection, `npm run build`, `npm run test` | Build passed; 178/178 tests passed | Live Supabase persistence smoke still required for every mutation type |
| Financial Settings navigation and working sections | PARTIAL | Added `/financial/settings` Admin/Auditor route and nav item with functioning forms for service pricing, discount rules, consumables, doctor compensation, payment-method visibility, external-cost defaults, exceptional-pricing/report links, and general rules. | `app/(crm)/financial/settings/page.tsx`, `app/(crm)/financial/settings/actions.ts`, `components/financial/FinancialSettingsManager.tsx`, `lib/nav.ts`, `lib/i18n/dictionaries.ts` | Existing finance tables plus migration `0014` template tables | `npm run build` | Page compiles and route is included in production build | Edit/deactivate UX exists for core active service rows, but default-template deactivate/edit is not yet a full inline editor; requires migration `0014` applied |
| Price all canonical services | PARTIAL | Service Pricing reads canonical booking/Admin `services` and overlays CRM financial settings without rewriting frozen lead records. | `lib/data/financialSettings.ts`, `components/financial/FinancialSettingsManager.tsx`, `lib/data/financialSettingsMutations.ts` | Booking `services`; CRM `crm_financial_service_settings` | Build/type validation | Build passed | Live Admin booking catalog smoke needed; service code column is not read because current booking query evidence did not prove a `code` column exists |
| Doctor dropdown source of truth | PASS | Financial doctor dropdowns read `financialDoctorCatalog()` from the Admin booking DB. Public booking still reads only active doctors; finance reads all doctors. | `lib/booking/service.ts`, `lib/data/financialSettings.ts`, `components/financial/FinancialSettingsManager.tsx`, `components/lead/PaymentsTab.tsx` | Booking `doctors`, `specialties` | Code inspection, `npm run build` | Build passed | Live adminaspectsclinica database smoke required to confirm row visibility in deployed Admin Dashboard |
| Add doctor from CRM defaults not bookable | PASS | `createFinancialDoctor()` inserts into booking `doctors` with `is_active: false`; public booking catalog filters `.eq("is_active", true)`, so the new finance doctor is not public until activated in Admin scheduling controls. | `lib/booking/service.ts`, `lib/data/financialSettingsMutations.ts`, `app/(crm)/financial/settings/actions.ts` | Booking `doctors.is_active` | Code inspection, `npm run build` | Build passed | Live insert test not run in sandbox to avoid polluting production/Admin data |
| Doctor compensation rules | PARTIAL | Settings supports doctor+service percentage or fixed rules with basis and active flag; lead-level compensation remains Admin/Auditor-only and hidden from moderators. | `components/financial/FinancialSettingsManager.tsx`, `lib/data/financialSettingsMutations.ts`, `components/lead/PaymentsTab.tsx`, `lib/data/financials.ts` | `crm_doctor_compensation_rules`, `crm_lead_doctor_compensation` | `npm run test`, `npm run build` | Engine tests cover percentage/fixed/multiple doctors; build passed | Need live CRUD smoke and explicit effective-date UI controls for comp rules |
| Moderator discount precedence | PASS | Existing `resolveMaxDiscount()` precedence remains moderator+service → moderator → service → global; Payments tab shows resolved ceiling/minimum price and server re-resolves before save. | `lib/financial/rules.ts`, `lib/data/financials.ts`, `components/lead/PaymentsTab.tsx`, `components/financial/FinancialSettingsManager.tsx` | `crm_discount_rules` | `npm run test` | Precedence unit tests passed | Live user+service rule smoke pending |
| Moderator Payments view and permissions | PASS | Moderator can view base price, quote, max discount, amount due, recorded payments, outstanding, save in-range quote, and request escalation; doctor compensation, consumables, doctor-funded payments, external costs, and profitability costs are hidden and server-gated by `financial.editRules`. | `components/lead/PaymentsTab.tsx`, `lib/data/financials.ts`, `lib/auth/permissions.ts` | Finance lead/cost tables | Code inspection, `npm run test`, `npm run build` | Build passed; permission tests passed | Browser role smoke pending |
| Quoted price save | PARTIAL | `saveQuote()` persists to `crm_lead_financials`, audits, logs timeline, blocks over-limit moderator save, and supports escalation/force approval. | `lib/data/financials.ts`, `app/(crm)/leads/financial-actions.ts`, `components/lead/PaymentsTab.tsx` | `crm_lead_financials`, `crm_discount_approvals`, `audit_logs`, `lead_timeline_events` | `npm run test`, `npm run build` | Quote/unit tests and build pass | Live drawer tab-switch/refresh persistence smoke pending |
| Consumable and external-cost defaults | PARTIAL | Added service-level default template tables and settings forms for adding defaults. Patient-specific lead cost writes are Admin/Auditor-only and audited. | `supabase/migrations/0014_financial_settings_templates.sql`, `components/financial/FinancialSettingsManager.tsx`, `lib/data/financials.ts` | `crm_service_consumable_defaults`, `crm_service_external_cost_defaults`, `crm_lead_consumables`, `crm_external_costs` | `npm run build` | Build passed | Inline edit/deactivate/remove UX for defaults remains limited; migration not applied here |
| Doctor-funded patient payment | PASS | Lead form now uses Admin doctor dropdown; server resolves doctor name from Admin catalog and stores doctor-funded payments separately from compensation. | `components/lead/PaymentsTab.tsx`, `lib/data/financials.ts`, `lib/booking/service.ts` | `crm_doctor_funded_payments`, booking `doctors` | `npm run build` | Build passed | Live entry smoke pending |
| Financial Dashboard and drilldowns | PASS | Dashboard uses canonical tables, labels cash-flow vs profitability basis, filters completed ledger rows for cash-flow, separately includes doctor-funded payments, and links cards to server-side drilldowns. | `app/(crm)/financial/page.tsx`, `lib/data/financialDashboard.ts` | Finance canonical tables | `npm run build` | Build passed | Drilldown row cap is 300 for responsiveness; live totals should be compared against Supabase reports |
| Financial audit logging | PARTIAL | Financial lead writes log to `crm_financial_audit_log` and lead timeline/audit; financial settings write to global `audit_logs`. | `lib/data/financials.ts`, `lib/data/financialSettingsMutations.ts` | `crm_financial_audit_log`, `audit_logs`, `lead_timeline_events` | Code inspection, build | Build passed | Need live Audit Logs smoke for every financial setting and mutation type |
| Lint/typecheck/build/tests | PARTIAL | Production build passes; tests pass when allowed to create the `tsx` IPC pipe. `next lint` is not configured and opens the interactive setup prompt. | `package.json`, app source | N/A | `npm run build`, `npm run test`, `npm run lint` | Build passed; tests passed 178/178 after escalation; lint blocked by missing ESLint config | Add non-interactive ESLint config/script in a separate cleanup |

| Requirement | Current state | Files/components involved | Database tables/functions involved | Implementation status | Test performed | Result | Remaining blocker |
|---|---|---|---|---|---|---|---|
| Canonical drawer tabs: Messenger and WhatsApp | The canonical lead drawer now uses Overview, Messenger, WhatsApp, Comments, Notes, Follow-Up, Booking, Payments / Financials, and Log. Messenger loads only Facebook Messenger/Instagram DM messages; WhatsApp loads WhatsApp messages only and shows an honest Not Configured state when server credentials are absent. Header external channel link remains directly under the lead name; Identity & technical no longer hides the primary chat link. | `components/lead/LeadDetail.tsx`, `components/lead/MessageThread.tsx`, `lib/loadLeadDetail.ts`, `app/api/leads/[id]/tab/route.ts`, `lib/whatsapp/config.ts` | `crm_messages`, `crm_message_attachments`, `crm_message_reactions`, `leads.chat_link` | Implemented, browser smoke pending | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Need authenticated browser verification that existing FB/IG messages render after the label change and cached tab return feels instant |
| WhatsApp integration path | Added secure server-only WhatsApp ingest endpoint for n8n-normalized records. It requires `WHATSAPP_INGEST_API_KEY` or `CRM_INGEST_API_KEY`, finds/creates canonical leads by phone/platform identity, dedupes by WhatsApp message ID, writes `crm_messages` and attachments, updates unread/contact timestamps, and logs a timeline event. No fake messages or connected status are shown. | `app/api/crm/ingest/whatsapp/route.ts`, `lib/whatsapp/config.ts`, `components/lead/LeadDetail.tsx` | `leads`, `crm_messages`, `crm_message_attachments`, `lead_timeline_events`, `crm_generate_lead_id` | Implemented structurally, no live credentials | `npm run build`, `npm run test` | Build passes; endpoint included in build routes | Need live WhatsApp/n8n credential smoke; outbound send is not implemented/enabled |
| Dedicated primary-stage navigation pages | Added `Qualified Leads` and `Post-Op Follow-Up` nav/pages, renamed Database Leads to Database, and locked New Leads to only `new`. Qualified page server-queries `qualified` + `booked`; Follow-Up page queues only regular follow-up; Post-Op queues only post-op; Database remains the unified searchable view with stage filter. | `lib/nav.ts`, `lib/i18n/dictionaries.ts`, `app/(crm)/leads/page.tsx`, `app/(crm)/qualified/page.tsx`, `app/(crm)/follow-up/page.tsx`, `app/(crm)/post-op/page.tsx`, `app/(crm)/database/page.tsx`, `components/leads/LeadsToolbar.tsx`, `lib/data/supabase.ts` | `leads.status`, `lead_follow_up_stages` | Implemented | `npm run build`, `npm run test` | Build passes; nav tests still pass | Need live browser smoke for New -> Qualified removal from New Leads and appearance in Qualified; follow-up queue should get pagination if it grows large |
| Tag settings and permissions | Tags can now be created/edited with name, color, active state, and numeric order by Admin/Auditor via existing Settings. Server action catches unexpected settings errors as form state instead of RSC digest. Moderator drawer remains limited to toggling active preconfigured tag IDs, enforced by `setLeadTagAssignments()`. | `components/settings/SettingsManager.tsx`, `app/(crm)/settings/actions.ts`, `lib/data/settingsData.ts`, `lib/data/settingsMutations.ts`, `components/lead/LeadDetail.tsx`, `lib/data/leadMutations.ts` | `lead_tags`, `lead_tag_assignments`, `audit_logs`, `lead_timeline_events`, migration `0012` | Implemented, live role smoke pending | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Need apply migration `0012`; live Admin/Auditor/Moderator permission smoke |
| Follow-up plan engine and full plan tab | Follow-up rules now support arbitrary active steps with name/order/delay/unit/anchor/status/tag applicability/version. Entering follow-up or post-op creates concrete snapshot rows when none exist; completed rows remain historical truth. Follow-Up tab lazy-loads and shows all concrete steps with due date, state, notes, completion timestamp/by, Mark done, Snooze 1 day, and Mark lost via mandatory Lost workflow. | `lib/data/followupPlans.ts`, `components/lead/LeadDetail.tsx`, `lib/data/leadMutations.ts`, `components/settings/SettingsManager.tsx`, `app/(crm)/settings/actions.ts`, `lib/data/settingsData.ts`, `lib/data/settingsMutations.ts`, `lib/loadLeadDetail.ts` | `crm_followup_workflow_stages`, `lead_follow_up_stages`, `lead_tags`, `lead_tag_assignments`, `audit_logs`, `lead_timeline_events`, migration `0012` | Implemented, live workflow smoke pending | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Need live acceptance for 3-step and 10-step plans, mark done/snooze/lost refresh persistence; no silent retroactive rebuild UI yet |
| CRM performance phase: drawer must open without loading full lead universe | Prompt 7 forensic audit created `PERFORMANCE_AUDIT.md`. Drawer shell now loads only the selected lead shell, active tags, and lost reasons; Messenger/WhatsApp/Comments/Booking/Payments/Log load lazily through `/api/leads/[id]/tab` and cache per lead/tab in the client. Duplicate member names no longer require `getLeads()` for all leads. | `PERFORMANCE_AUDIT.md`, `lib/loadLeadDetail.ts`, `components/lead/LeadDetail.tsx`, `app/api/leads/[id]/tab/route.ts` | `leads`, `lead_tag_assignments`, `lead_tags`, `lost_reasons`, deferred tab tables | Implemented, browser smoke pending | `npm run build`, `npm run test`; live Supabase timing probes | Build passes; 178/178 tests pass; new shell measured as 2359ms + 711ms remote query rounds on current 26-lead DB | Authenticated browser timing still needed; large histories still need per-tab pagination/infinite loading beyond this shell split |
| Lead lists must scale past 20,000 leads | `/leads` and `/database` now use `getLeadsPage()` with 30-row server-side `range()` pagination and exact count; search/filter stays in Supabase instead of downloading all leads. Calendar no longer calls `getLeads()` to map booking IDs. | `app/(crm)/leads/page.tsx`, `app/(crm)/database/page.tsx`, `app/(crm)/calendar/page.tsx`, `lib/data/contracts.ts`, `lib/data/index.ts`, `lib/data/supabase.ts` | `leads`, `lead_tag_assignments`, `lead_follow_up_stages` | Implemented | Live Supabase probes and build/tests | Direct 30-row list query measured 2534ms on 26-lead DB; search query 2446ms; build/tests pass | Need apply migration `0011` and re-run explain/analyze on staging/prod-size data; current live DB has only 26 leads |
| Performance indexes for search/list/history | Added additive migration with `pg_trgm`, lead sort/filter indexes, search GIN indexes, and drawer history indexes. | `supabase/migrations/0011_crm_performance_indexes.sql` | `leads`, `lead_timeline_events`, `audit_logs`, `lead_follow_up_stages` | Added, not applied in this workspace | SQL/code inspection; build unaffected | Migration is additive and idempotent (`if not exists`) | Must apply to production Supabase and monitor write overhead |
| Status/tag production Server Component digest errors | Root cause from code inspection: status/tag actions revalidated broad paths that re-rendered the eager full drawer; ordinary server errors escaped the action state boundary. Drawer is now lazy and `LeadActionState` returns useful errors for normal `Error`s. Status changes now require confirmation showing patient, old status, and proposed new status before mutation. | `components/lead/LeadDetail.tsx`, `app/(crm)/leads/actions.ts`, `lib/loadLeadDetail.ts` | `leads`, `lead_status_history`, `audit_logs`, `lead_timeline_events`, `lead_tag_assignments` | Fixed in code, production digest not log-confirmed | Build/tests | Build passes; tests pass | Need production log/digest confirmation and browser status/tag persistence smoke |
| Notes must save only after real persistence | Notes write now updates `updated_at`, requests the updated row with `.select("id").single()`, and reports success only after update + audit + timeline succeed. | `components/lead/NotesTab.tsx`, `app/(crm)/leads/actions.ts`, `lib/data/leadMutations.ts` | `leads.notes`, `leads.medical_history`, `leads.medical_notes`, `audit_logs`, `lead_timeline_events` | Tightened | Build/tests | Build passes; tests pass | Browser refresh persistence smoke still needed |
| Payment entry client exception and stale totals | Root cause found in dev log: `financial-actions.ts` was a `"use server"` file exporting non-function `IDLE`, which Next can reject when the Payments client imports actions. Initial action state now lives in the client component, and successful financial actions invalidate/refetch only the Payments tab cache instead of relying on whole drawer revalidation. Same cleanup was applied to Settings, Users, and Auditor action-state constants. | `components/lead/PaymentsTab.tsx`, `components/lead/LeadDetail.tsx`, `app/(crm)/leads/financial-actions.ts`, `app/(crm)/settings/actions.ts`, `app/(crm)/settings/users/actions.ts`, `app/(crm)/auditor/actions.ts`, `lib/data/financials.ts` | `crm_financial_transactions`, `crm_lead_financials`, `crm_financial_audit_log`, `audit_logs`, `lead_timeline_events` | Fixed in code, browser smoke pending | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Browser payment entry + refresh totals smoke still needed |
| Live booking server exception | Booking slot/create/status server actions now convert unexpected `Error`s into form state instead of escaping to the server-error page. Booking catalog is lazy-loaded only when Booking tab opens. | `app/(crm)/booking/actions.ts`, `components/lead/BookingTab.tsx`, `components/lead/LeadDetail.tsx`, `lib/booking/service.ts` | Booking Supabase `appointments`, `doctor_schedule_templates`, `blocked_times`, `services`, `clinic_settings` | Improved | Build/tests | Build passes; tests pass | Need authenticated browser live-slot smoke and production env verification |
| Production data source must not silently fall back to mock/demo data | `CRM_DATA_SOURCE` previously defaulted to mock unless explicitly set to `supabase` | `lib/data/index.ts`, `lib/data/source.ts`, `.env.example` | All CRM tables through data provider | Fixed in Prompt 1 | `npx tsc --noEmit`, `npm run test`, `npm run build` | Source defaults to Supabase; `CRM_DATA_SOURCE=mock` throws in production; tests added | Confirm Coolify does not set `CRM_DATA_SOURCE=mock` |
| Local and deployed schema must match code expectations | Code selected duplicate confidence and escalation severity columns not guaranteed by baseline | `lib/data/supabase.ts`, `supabase/migrations/0008_crm_foundational_persistence.sql` | `lead_duplicate_flags.confidence_score`, `escalations.severity`, `lead_sources` | Fixed in Prompt 1 with additive migration | `npx tsc --noEmit`, `npm run build` | Build passes | Apply migration `0008` to production Supabase before deploying code that writes `escalations.severity` |
| Notes must actually persist after tab switches, drawer close, refresh, logout/login, redeploy | Notes tab only set local `Saved` state and never called database | `components/lead/NotesTab.tsx`, `app/(crm)/leads/actions.ts`, `lib/data/leadMutations.ts`, `lib/loadLeadDetail.ts` | `leads.notes`, `leads.medical_notes`, `leads.medical_history`, `audit_logs`, `lead_timeline_events` | Fixed in Prompt 1 | Typecheck/build/tests; code path awaits update + audit + timeline | UI shows success only after server action returns success | Needs browser smoke against live Supabase session to verify refresh persistence |
| Status changes must persist and refresh related views | Lead drawer status pills were local-only | `components/lead/LeadDetail.tsx`, `app/(crm)/leads/actions.ts`, `lib/data/leadMutations.ts` | `leads.status`, `lead_status_history`, `audit_logs`, `lead_timeline_events` | Fixed in Prompt 1 for basic status changes | Typecheck/build/tests | Server action updates DB and revalidates lead/list/dashboard/follow-up/escalations/duplicates | Needs live browser smoke for each stage |
| Mark Lost must require reason and display Lost as red/bold | UI now opens mandatory modal, server requires active reason, Lost badge/pill are red, overview shows persisted reason | `components/lead/LeadDetail.tsx`, `lib/data/leadMutations.ts`, `lib/badges.ts`, `supabase/migrations/0009_lead_workflows_persistence.sql` | `leads.status`, `leads.lost_reason_id`, `leads.lost_notes`, `lost_reasons`, `audit_logs`, `lead_timeline_events` | Fixed for lead drawer workflow; settings/report surfacing partial | `npx tsc --noEmit` | Passes; migration seeds requested reason examples | Need live browser persistence smoke and auditor lost-leads/report consumption |
| Escalate must require reason, persist, and button becomes Un-Escalate | Header button previously did nothing | `components/lead/LeadDetail.tsx`, `app/(crm)/leads/actions.ts`, `lib/data/leadMutations.ts` | `escalations`, `leads.escalation_status`, `audit_logs`, `lead_timeline_events` | Fixed for create and clear in Prompt 1 | Typecheck/build/tests | Modal requires reason; insert/update awaited; errors shown | Need live browser smoke; auditor resolution choices still incomplete |
| Escalation resolution must support send back to moderator and completely resolved | Escalations queue and drawer Log panel now expose both actions with note field, pending/error state | `app/(crm)/escalations/page.tsx`, `app/(crm)/escalations/actions.ts`, `components/queues/EscalationResolutionControls.tsx`, `components/lead/LeadDetail.tsx`, `lib/data/leadMutations.ts` | `escalations`, `leads.escalation_status`, `leads.has_unread`, `leads.unread_since`, `crm_unread_events`, `audit_logs`, `lead_timeline_events` | Implemented, not live-smoked | `npx tsc --noEmit` | Passes; return path marks lead unread/action-required and logs event | Need browser/live Supabase test for counter/list refresh and role permissions |
| User edit audit logging must record actor, timestamp, entity, action, field, old and new values | Notes/status/tags/new lead/escalation/follow-up/duplicate merge/booking creation/reservation status/scheduling edits/payment writes now write audit and/or lead timeline entries through server mutations | `lib/data/leadMutations.ts`, `lib/data/supabase.ts`, `lib/booking/service.ts`, `lib/data/financials.ts`, `app/(crm)/leads/actions.ts`, `app/(crm)/booking/actions.ts`, `app/(crm)/settings/scheduling-actions.ts` | `audit_logs`, `lead_timeline_events`, `duplicate_review_actions`, `crm_financial_audit_log`, `appointment_status_history`, `crm_unread_events` | Partial but expanded in Prompt 2 | `npx tsc --noEmit` | Passes | Still need audit coverage for reports, settings domains outside scheduling, and live outgoing send path |
| Authenticated user must be used for writes, not hard-coded actor | Duplicate/escalation queue writes used `CURRENT_USER_ID` | `lib/data/supabase.ts`, `lib/data/actor.ts`, `lib/data/session.ts` | `crm_users`, `audit_logs`, `duplicate_review_actions`, `escalations` | Fixed for duplicate/escalation queue actions in Prompt 1 | Typecheck/build/tests | Hard-coded actor removed from those actions | Need review every remaining write path for server-side permission checks |
| Moderators must not create arbitrary tags | Lead drawer allowed free-text local tag creation | `components/lead/LeadDetail.tsx`, `lib/data/leadMutations.ts`, `lib/data/supabase.ts`, `lib/loadLeadDetail.ts` | `lead_tags`, `lead_tag_assignments`, `audit_logs`, `lead_timeline_events` | Fixed for lead drawer in Prompt 1 | Typecheck/build/tests | UI only toggles active configured tags; server validates active tag IDs | Need settings UI for authorized tag management |
| Assigned tags must render from Supabase | Tags always mapped to empty list | `lib/data/supabase.ts` | `lead_tag_assignments`, `lead_tags` | Fixed in Prompt 1 | Typecheck/build/tests | Lead list/detail mapper loads tag names | Needs live data smoke |
| + New Lead must create a real lead | Button existed but had no handler | `app/(crm)/leads/page.tsx`, `components/leads/NewLeadButton.tsx`, `app/(crm)/leads/actions.ts`, `lib/data/leadMutations.ts` | `leads`, `crm_generate_lead_id`, `audit_logs`, `lead_timeline_events` | Fixed basic manual creation in Prompt 1 | Typecheck/build/tests | Form posts to Supabase and navigates to created lead after success | Need richer create flow for MRN, source attribution, tags, duplicate check review |
| Search must work across Lead ID, MRN, phone, name, chat link, platform ID | Toolbar has explicit Search button and Enter submit; adapter searches all requested columns and digit-normalized phone variant | `components/leads/LeadsToolbar.tsx`, `lib/data/supabase.ts` | `leads.lead_id`, `leads.mrn`, `leads.normalized_phone`, `leads.phone_number`, `leads.platform_id`, `leads.chat_link` | Implemented, not live-smoked | `npx tsc --noEmit` | Passes | Need browser/live verification with real phone/chat/platform ID samples |
| Database must not be a placeholder | Database page uses the same server-paginated leads table, toolbar, filters, and search parser as the dedicated lead pages, but remains the unified all-stage database view | `app/(crm)/database/page.tsx`, `components/leads/LeadsToolbar.tsx`, `components/leads/LeadsTable.tsx` | `leads`, `lead_sources` | Fixed read/filter view | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Need browser/live search/filter smoke and history drilldowns |
| Platform and Source filters should be merged | Toolbar now exposes one `Channel / Source` filter using platform keys plus live active `lead_sources` UUIDs | `components/leads/LeadsToolbar.tsx`, `app/(crm)/leads/page.tsx`, `app/(crm)/database/page.tsx`, `lib/data/supabase.ts` | `lead_sources`, `leads.platform`, `leads.source_id` | Implemented | `npx tsc --noEmit` | Passes | Need live browser smoke; campaign options are still free text/query driven, not a dropdown |
| Lead drawer tab name must be Log, not Audit | Drawer used `Audit` label | `components/lead/LeadDetail.tsx` | `lead_timeline_events`, `audit_logs`, `escalations`, `lead_duplicate_flags` | Fixed label in Prompt 1 | Build | Drawer compiles with `Log` tab | Need screenshot-level typography match for Log timeline |
| Direct channel link must be under lead name | Link was hidden in Identity & technical | `components/lead/LeadDetail.tsx` | `leads.chat_link`, `lead_source_links`, `crm_conversations` | Fixed basic placement in Prompt 1 | Build | Header renders Open Facebook/Instagram/WhatsApp link when `chatLink` exists | Need conversation-specific fallback/page inbox link preference |
| Messenger tab should match requested Khalid Nasser layout and AI suggested-reply dock | Bubbles now use incoming white/left and outgoing blue/right with timestamps/channel/waiting indicator; bottom review area added but direct AI regenerate/send remains disabled because no connected send/regenerate handler is exposed here | `components/lead/MessageThread.tsx`, `components/lead/LeadDetail.tsx` | `crm_messages`, `crm_message_attachments`, `crm_message_reactions`, `crm_ai_reply_logs`, `ai_reply_suggestions`, `crm_settings.ai_reply_assistant` | Partial UI implementation, action integration open | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Need wire actual AI draft generation/logging and real channel send/log action before enabling buttons |
| Comments tab must show associated Facebook/Instagram comments and count | Comments panel exists; count shown in tab | `components/lead/CommentsPanel.tsx`, `components/lead/LeadDetail.tsx`, `lib/data/supabase.ts` | `crm_comments`, `crm_comment_attachments` | Existing partial | Build/tests | Mapper compiles; ingest tests cover comments | Need live association smoke and unread/comment counter UX |
| Follow-Up drawer tab must be useful: schedule, mark done, snooze, next step, lost | Drawer can schedule next, mark active follow-up done, snooze one day, and invoke mandatory Lost modal | `components/lead/LeadDetail.tsx`, `app/(crm)/leads/actions.ts`, `lib/data/leadMutations.ts`, `lib/data/supabase.ts` | `lead_follow_up_stages`, `leads.status`, `audit_logs`, `lead_timeline_events` | Implemented basic workflow | `npx tsc --noEmit` | Passes; adapter no longer selects nonexistent follow-up columns | Need browser/live persistence smoke; automated tests still needed for follow-up mutations |
| Follow-Up page click must open lead drawer on Follow-Up tab | Previously opened overview | `components/queues/LeadCell.tsx`, `app/(crm)/follow-up/page.tsx`, `components/lead/LeadDetail.tsx` | None | Fixed in Prompt 1 | Build | Links include `?tab=Follow-Up`; drawer reads tab param | Browser smoke needed |
| Follow-up settings must be configurable by admin/auditor | Settings now edits flexible follow-up rules with arbitrary steps, delay units, anchors, status/tag applicability, active state, and versioning; concrete lead plan rows snapshot the rule when created | `app/(crm)/settings/page.tsx`, `components/settings/SettingsManager.tsx`, `lib/data/settingsData.ts`, `lib/data/settingsMutations.ts`, `supabase/migrations/0012_crm_whatsapp_followup_settings.sql` | `crm_followup_workflow_stages`, `lead_follow_up_stages`, `lead_tags`, `lead_tag_assignments`, `audit_logs` | Implemented, live smoke pending | `npm run build`, `npm run test` | Build passes; 178/178 tests pass | Need apply migration `0012`; live Admin/Auditor save smoke and retroactive-apply UX if desired |
| Duplicate merge must preserve all linked data | Merge decision now calls transactional SQL RPC that reassigns messages, comments, source links, tags, follow-ups, escalations, timeline/audit-related rows, attribution, and non-conflicting financial record; duplicate lead is marked `merged_into_lead_id` | `components/queues/DuplicateCard.tsx`, `components/lead/LeadDetail.tsx`, `app/(crm)/duplicates/actions.ts`, `lib/data/supabase.ts`, `supabase/migrations/0009_lead_workflows_persistence.sql` | `crm_merge_duplicate_flag`, `leads.merged_into_lead_id`, `lead_duplicate_flags`, `duplicate_review_actions`, `crm_messages`, `lead_messages`, `crm_comments`, `lead_source_links`, `lead_tag_assignments`, `lead_follow_up_stages`, `escalations`, `crm_lead_financials`, `audit_logs`, `lead_timeline_events` | Implemented conservative merge, not applied/tested live | `npx tsc --noEmit` | TypeScript passes; SQL not yet executed against Supabase in this session | Need apply migration and test merge in live DB; merge blocks if both leads already have financial ledgers requiring manual reconciliation |
| Duplicates page buttons must not be dead | Queue and drawer Log duplicate buttons call server actions with pending/error states; merge path invokes transactional RPC | `components/queues/DuplicateCard.tsx`, `components/queues/ResolveButton.tsx`, `components/lead/LeadDetail.tsx`, `app/(crm)/duplicates/actions.ts` | `lead_duplicate_flags`, `duplicate_review_actions`, `crm_merge_duplicate_flag` | Implemented, live test pending | `npx tsc --noEmit` | Passes | Need live merge/not-duplicate/link smoke and clearer linked identity status in UI |
| Booking tab inside lead drawer must match supplied public booking flow/UI | Drawer now reuses the booking platform catalog/availability rules and renders the supplied flow: specialty/doctor, selected doctor card, branch, service/procedure, date calendar, live slots, patient info, review, and confirm. Old disconnected layout removed. | `components/lead/BookingTab.tsx`, `components/lead/LeadDetail.tsx`, `app/(crm)/booking/actions.ts`, `lib/booking/service.ts`, PDF reference `2026-07-10_22.05.13.pdf` | Booking Supabase `appointments`, `appointment_status_history`, `doctor_schedule_templates`, `blocked_times`, `services`, `clinic_settings`; CRM `leads.booking_appointment_id`, `audit_logs`, `lead_timeline_events` | Implemented, live smoke pending | `npm run build` | Passes; server action inserts only after live slot check and DB insert success | Need live browser create-booking smoke and verify room-assignment parity if the public booking admin depends on `appointment_rooms` |
| Public booking, CRM Booking tab, Reservations, Calendar, Scheduling must use one availability source | Public site, CRM Booking tab, Reservations, Calendar, and Settings Scheduling all read/write the booking Supabase source-of-truth tables; no new CRM schedule table was introduced | `lib/booking/client.ts`, `lib/booking/service.ts`, `lib/booking/reservations.ts`, `lib/booking/sync.ts`, `app/(crm)/reservations/page.tsx`, `app/(crm)/calendar/page.tsx`, `components/settings/SchedulingSettings.tsx`, public `src/app/api/book/route.ts` | Booking DB `appointments`, `appointment_status_history`, `doctor_schedule_templates`, `blocked_times`, `clinic_settings`, `services`, `doctors`, `branches`; CRM `leads.booking_appointment_id` | Implemented architecture, live smoke pending | Public booking audit; `npx tsc --noEmit` | Code paths now share booking DB and slot logic | Public booking app still does not directly POST to CRM ingest; CRM mitigates with sync-on-read from booking DB, but production should still wire sender/n8n for immediate unread notifications |
| Website Reservations must show new public bookings and link to canonical lead drawer | Page/nav are renamed Website Reservations. Reservations read live booking appointments, sync/link matching CRM leads by booking ID or phone, create a lead when needed, mark awaiting-confirmation reservations new/unread, and rows link to `/leads/{id}?tab=Booking` with status controls. | `app/(crm)/reservations/page.tsx`, `components/booking/ReservationStatusSelect.tsx`, `lib/booking/reservations.ts`, `lib/booking/sync.ts`, `app/(crm)/booking/actions.ts`, `app/api/ingest/reservation/route.ts`, `lib/reservationStatus.ts`, `lib/nav.ts`, `lib/i18n/dictionaries.ts` | Booking `appointments`, `appointment_status_history`; CRM `leads.booking_appointment_id`, `leads.normalized_phone`, `audit_logs`, `lead_timeline_events` | Implemented, live smoke pending | `npm run build` | Passes; click path opens canonical drawer Booking tab | Need production live booking submission smoke and confirm unread counters refresh in shell after sync |
| Bulk Import must support CSV/Excel, mapping, preview, validation, confirmation, and canonical writes | Added dedicated `/bulk-import` nav/page. Client parses CSV/TSV/text and `.xlsx`, supports drag/drop, header detection, manual mapping, preview, validation counts, duplicate warnings, and explicit confirmation before commit. Server action resolves existing leads first, creates canonical manual leads only with safe Name+Phone identity, writes quote/payment through canonical finance actions, flags unsupported complex columns for review, and logs import batches. | `app/(crm)/bulk-import/page.tsx`, `components/financial/BulkImport.tsx`, `app/(crm)/financial/import/actions.ts`, `lib/financial/importMapping.ts`, `app/(crm)/financial/page.tsx`, `app/(crm)/financial/import/page.tsx` | `leads`, `lead_sources`, `crm_lead_financials`, `crm_financial_transactions`, `audit_logs`, `lead_timeline_events`, `crm_generate_lead_id` | Implemented, browser file smoke pending | `npm run build`; `npm run test` attempted | Build passes; current test run blocked by sandbox IPC + rejected escalation usage limit | Need authenticated CSV/XLSX upload smoke and live import rollback/cleanup plan for test rows |
| Booking ingest receiver must not create duplicates | Receiver remains idempotent; additionally Reservations/Calendar now perform sync-on-read idempotently by booking appointment ID and normalized phone | `app/api/ingest/reservation/route.ts`, `lib/booking/sync.ts`, `app/(crm)/reservations/page.tsx`, `app/(crm)/calendar/page.tsx` | `leads.booking_appointment_id`, `leads.normalized_phone`, `crm_generate_lead_id`, `audit_logs`, `lead_timeline_events` | Improved but sender still external | `npx tsc --noEmit` | Sync code compiles and uses existing canonical lead IDs | Need public booking sender/n8n production verification; sync-on-read is a safety net, not push notification delivery |
| Calendar must be Saturday first, Friday last | Calendar was Sunday first | `app/(crm)/calendar/page.tsx` | Booking `appointments`; CRM `leads.booking_appointment_id` | Fixed in Prompt 1 | Build | Grid is Sat->Fri | Need visual browser smoke |
| Calendar must show Lead ID above name | Calendar links/syncs booking appointments to CRM leads and renders Lead ID above patient name when linked | `app/(crm)/calendar/page.tsx`, `lib/booking/sync.ts` | CRM `leads.booking_appointment_id`; Booking `appointments` | Fixed when link exists; sync now creates/links leads from booking rows | `npx tsc --noEmit` | Passes; Lead ID is visible in appointment card before name | Need live browser smoke with real reservations |
| Calendar must group all appointments by department/doctor and show all | Month cells show all appointments in scrollable cells; a grouped operations section groups live bookings by specialty/department then doctor | `app/(crm)/calendar/page.tsx` | Booking `appointments`, `doctors`, `specialties`, `services`; CRM `leads.booking_appointment_id` | Implemented, live smoke pending | `npx tsc --noEmit` | Passes | Need browser visual check for dense days and mobile layout |
| Scheduling settings must edit the same scheduling system used by public booking | Settings now includes Scheduling section reading booking DB rules/schedules/blocks and authorized actions for schedule hours/active status/first-come capacity plus blocked date/time add/remove | `app/(crm)/settings/page.tsx`, `app/(crm)/settings/scheduling-actions.ts`, `components/settings/SchedulingSettings.tsx`, `lib/booking/service.ts` | Booking `doctor_schedule_templates`, `blocked_times`, `clinic_settings`; CRM `audit_logs` | Implemented for existing schedule rows and blocked times | `npx tsc --noEmit` | Passes; writes are server actions and require authenticated Admin/Auditor-level rule capability | Need add/create doctor schedule rows from CRM if admins should not use booking admin for first-time schedule creation |
| Payments tab must work in production | Financial load errors are no longer swallowed; Payments tab displays the exact Supabase read failure, and all payment/quote/cost mutations write both financial audit and lead Log entries | `components/lead/PaymentsTab.tsx`, `app/(crm)/leads/financial-actions.ts`, `lib/data/financials.ts`, `lib/loadLeadDetail.ts` | `crm_lead_financials`, `crm_financial_transactions`, `crm_financial_audit_log`, `crm_discount_approvals`, `crm_doctor_funded_payments`, `crm_lead_consumables`, `crm_external_costs`, `audit_logs`, `lead_timeline_events` | Root-cause visibility/logging fixed, live production failure still needs deployed smoke | `npx tsc --noEmit` | Passes | Need test against deployed Coolify environment to capture the exact production Supabase error if it persists |
| Settings must include the complete requested CRM structure without fake empty tabs | Settings now contains General CRM Settings, Lead Fields, Tags & Colors, Lost Reasons, Escalation Reasons, Follow-Up Stages, Rules, Sources & Campaigns, ID/MRN Rules, Duplicate Rules, Reporting Settings, AI Reply Assistant, Integrations, Scheduling, Financial Settings, and Email Rules. Real editors persist tags, lost reasons, escalation reasons, SLA/stage rules, follow-up stages, reporting targets, AI prompt, email rules, and scheduling; read-only panels disclose hardcoded operational rules instead of pretending they are editable. | `app/(crm)/settings/page.tsx`, `components/settings/SettingsManager.tsx`, `app/(crm)/settings/actions.ts`, `lib/data/settingsData.ts`, `lib/data/settingsMutations.ts`, `lib/data/emailRuleMutations.ts`, `lib/loadLeadDetail.ts`, `components/lead/LeadDetail.tsx`, `supabase/migrations/0013_crm_escalation_reasons.sql` | `lead_tags`, `lost_reasons`, `crm_escalation_reasons`, `crm_stage_reply_rules`, `crm_followup_workflow_stages`, `auditor_settings`, `ai_prompt_templates`, `crm_email_rules`, booking scheduling tables, `audit_logs` | Implemented, live smoke pending | `npm run build`; `npm run test` attempted | Build passes; current test run blocked by sandbox IPC + rejected escalation usage limit | Apply migration `0013`; live persistence smoke for every editable tab; deep Arabic string coverage remains incremental |
| Auditor settings must feed the dashboard | Target CPL edited in Settings is read by the auditor auto-snapshot and red-flag detection | `lib/data/settingsMutations.ts`, `lib/data/auditor.ts`, `lib/auditor/kpi.ts` | `auditor_settings.target_cpl_egp` | Implemented | `npm run test` (kpi unit tests) | Target CPL flows into CPL red-flag logic | Live smoke |
| Users & Roles must show Supabase Auth users newly added in Authentication | Prompt 6: `listUnlinkedAuthUsers()` reads Supabase Auth via the service-role Admin API on the **server only** (key never reaches the browser), lists auth accounts with no `crm_users` profile, and `linkAuthUser()` (admin-only `users.invite`, audit-logged) creates + links a profile with a chosen role. Rendered on the Users & Roles page. | `app/(crm)/settings/users/page.tsx`, `app/(crm)/settings/users/UnlinkedAuthUsers.tsx`, `app/(crm)/settings/users/actions.ts`, `lib/data/users.ts` | `crm_users`, Supabase Auth `auth.users` (Admin API), `audit_logs` | Implemented, live smoke pending | `npx tsc --noEmit`, `npm run build`, `npm run test` | Server-side admin API read; linking gated + logged; duplicates refused | Live smoke needs a real Supabase Auth project with an unlinked user |
| Reports and Auditor dashboards must generate and store meaningful reports | Prompt 5: full previous-day workflow — Generate computes real KPIs from that day's leads/escalations/payments; per-metric overrides require a reason and are logged (old/new); Finalize submits, publishes a persistent narrative to the Reports list, and can be reopened; Dropped/Lost count drills down to the actual leads + mandatory lost reasons; red flags highlighted. KPI formulas are pure + unit-tested. | `app/(crm)/auditor/page.tsx`, `components/auditor/AuditorReport.tsx`, `app/(crm)/auditor/actions.ts`, `lib/data/auditor.ts`, `lib/auditor/kpi.ts` (+`kpi.test.ts`), `app/(crm)/reports/page.tsx` | `audit_daily_reports`, `audit_metric_snapshots`, `audit_metric_overrides`, `operational_summary_reports`, `auditor_settings`, `leads`, `escalations`, `crm_financial_transactions`, `audit_logs` | Implemented, live smoke pending | `npx tsc --noEmit`, `npm run build`, `npm run test` (KPI unit tests pass) | Generate/override/finalize/reopen are permission-gated (`reports.generate`) and audit-logged; finalized report persists to Reports | Live Supabase persistence smoke; doctor/specialty scoping of the snapshot is not yet wired (date filter is) |
| Email automation tab and logs | Prompt 5: new Emails nav + page showing the append-only send log (recipients, subject, rule/trigger, linked lead, status, provider message id, error) — status reflects only the real Resend response, never fabricated. Flexible rule engine with triggers (booking_created, booking_under_review, overdue_leads_daily, custom) and recipient kinds incl. `moderators`/`auditors`/`static`/`involved_lead`. Idempotency via a unique dedupe key. Resend transport is guarded (unset key → logged `skipped`). Daily overdue summary via token-guarded cron. booking_created fires from both the CRM booking action and the public-reservation ingest. Rule editor lives in Settings. | `app/(crm)/emails/page.tsx`, `lib/email/rules.ts` (+`rules.test.ts`), `lib/email/send.ts`, `lib/email/resend.ts`, `lib/email/triggers.ts`, `lib/data/emailData.ts`, `app/api/cron/overdue-emails/route.ts`, `app/(crm)/booking/actions.ts`, `app/api/ingest/reservation/route.ts` | `crm_email_rules`, `crm_email_log` (unique `dedupe_key`), `supabase/migrations/0010_email_automation_and_settings.sql`, `audit_logs`, `leads`, `crm_users` | Implemented, live send smoke pending | `npx tsc --noEmit`, `npm run build`, `npm run test` (dedupe/template unit tests pass) | Engine + log + idempotency work; rules seeded disabled by default | Apply migration 0010; set `RESEND_API_KEY`/`EMAIL_FROM`; schedule the 09:00 cron; `involved_lead` cannot resolve until patient email is captured (schema stores phone only) |
| Arabic/English, RTL/LTR, Light/Dark mode | Prompt 6: genuine theming — semantic Tailwind tokens (surfaces/text/lines/primary/danger) converted to CSS variables with light+dark values, so a single `[data-theme]` on `<html>` re-themes the whole app (financial UI included) via existing utilities; verified in compiled CSS (`.bg-panel` → `rgb(var(--c-panel)/…)`, dark block present, no `<alpha-value>` leak). Locale (en/ar) + theme persisted in cookies and SSR-applied to `<html lang dir data-theme>` (no flash); Arabic sets `dir="rtl"` document-wide. Sidebar language + theme switch. Translation dictionary covers the shell/nav + activity log; extensible via `lib/i18n/dictionaries.ts`. | `tailwind.config.ts`, `app/globals.css`, `app/layout.tsx`, `app/(crm)/layout.tsx`, `lib/i18n/{config,server,context,dictionaries}.*`, `components/shell/PreferencesMenu.tsx`, `components/shell/Sidebar.tsx`, `lib/nav.ts` | Cookies `crm_locale`, `crm_theme` (no DB) | Implemented | `npx tsc --noEmit`, `npm run build` (+ compiled-CSS inspection), `npm run test` | Whole-app dark theme + RTL work; preference persists across requests | Full Arabic string coverage is incremental (shell/nav done, deep pages still English); inline hex in a few status chips does not theme; authenticated browser smoke needs live Supabase |
| Audit Logs global activity viewer | `/audit-logs` page (admin/auditor, `audit.view`) renders the unified `audit_logs` via `listActivity` with exact timestamp, user, role, action, entity, lead, old/new diff, source/reason, and filters for date, user id, role, action, entity type, and Lead ID. Legacy `/activity` redirects to `/audit-logs`; Moderator is denied by permission matrix/nav roles. | `app/(crm)/audit-logs/page.tsx`, `app/(crm)/activity/page.tsx`, `lib/audit/log.ts`, `lib/nav.ts` | `audit_logs`, `crm_users` | Implemented, live role smoke pending | `npm run build` | Reads the same sink every mutation writes to; route builds | Live Admin/Auditor/Moderator visibility smoke pending |
| Auditor snapshot doctor/specialty scoping | Prompt 6: `computeAutoSnapshot(date, scope)` filters leads/escalations/payments by `leads.doctor_id`; doctor + specialty selectors on the dashboard map onto doctor-id sets (specialty → its doctors) from the booking catalog. Produces a live filtered breakdown; the saved daily report stays clinic-wide. Dropped-lead drilldown honors the scope. | `lib/data/auditor.ts`, `app/(crm)/auditor/page.tsx`, `components/auditor/AuditorReport.tsx` | `leads.doctor_id`, booking catalog (doctors/specialties) | Implemented, live smoke pending | `npx tsc --noEmit`, `npm run build` | Real scoping via doctor_id; specialty expands to its doctors | Scope applies to the live view, not the persisted clinic-wide report (by design) |
| Financial system forensic audit (Phase 0) | Prompt 6 audit: the financial source-of-truth is **already implemented** and reused (not rebuilt) — central engine `lib/financial/{engine,quote,money,rules}.ts` (with unit tests), data layer `lib/data/financials.ts` (1522 lines), server actions `financial-actions.ts` (saveQuote, addTransaction, setTransactionStatus, addDoctorFunded, addConsumable, addExternalCost, requestApproval, decideApproval), and `PaymentsTab.tsx`. Discount precedence in `rules.ts` matches spec §4 exactly (moderator+service → moderator → service → global). No stub/mock/placeholder financial handlers found. The new theming makes the financial UI dark/RTL-aware. | `lib/financial/*`, `lib/data/financials.ts`, `app/(crm)/leads/financial-actions.ts`, `components/lead/PaymentsTab.tsx` | `crm_lead_financials`, `crm_financial_transactions`, `crm_discount_rules`, `crm_discount_approvals`, `crm_consumable_components`, `crm_lead_consumables`, `crm_doctor_compensation_rules`, `crm_lead_doctor_compensation`, `crm_doctor_funded_payments`, `crm_external_costs`, `crm_financial_service_settings`, `crm_lead_bundle_items`, `crm_financial_audit_log` | Pre-existing; audited, not rebuilt | Code inspection + `npm run build`/`test` | Comprehensive financial system present and reused per Phase 0 | Standalone Financial Dashboard + bulk-import UI + full 60-point live acceptance run are a separate multi-session effort; live persistence smoke needs deployed Supabase |
| RLS and permissions must be enforced functionally | Prompt 5: permission matrix extended with `settings.view/manage`, `reports.view/generate`, `email.view/manage` (admin + auditor; moderators denied). Every new settings/auditor/email server mutation resolves the real actor via `writeActor()` then `assertCan(...)` before writing — UI view-only state is not the boundary. | `lib/auth/permissions.ts`, `lib/data/settingsMutations.ts`, `lib/data/emailRuleMutations.ts`, `lib/data/auditor.ts`, action files | RLS select-only for authenticated active users; service-role bypass on server behind capability checks | Improved | `npm run test` (permission + nav tests pass) | New capabilities enforced server-side in each action | Financial/lead-write paths still to be re-audited for the same pattern; add automated action-level permission tests |
| Complete user-edit audit logging | Prompt 5: shared `logActivity()` sink writes settings, follow-up-rule, target-CPL, AI-prompt, auditor generate/override/finalize/reopen, and email-rule/send events to `audit_logs`, distinguishing system vs human (`metadata.source`, null actor for automation). `listActivity()` reader added. | `lib/audit/log.ts`, `lib/data/settingsMutations.ts`, `lib/data/emailRuleMutations.ts`, `lib/data/auditor.ts`, `lib/email/send.ts` | `audit_logs` | Expanded | `npx tsc --noEmit`, `npm run test` | All new operational writes are logged with actor/old/new; a failed audit write fails the operation | Prompt 6 added the `/activity` viewer over this sink; lead-level financial mutations still route through the pre-existing `crm_financial_audit_log` in addition |
| Environment-variable usage must be explicit | CRM uses multiple server/browser env vars; booking integration uses separate booking Supabase vars | `.env.example`, `lib/supabase/server.ts`, `lib/supabase/session.ts`, `lib/booking/client.ts`, ingest routes, public booking `src/lib/supabase/config.ts` | N/A | Documented in this checklist | Build with `.env.local` loaded | Build passes after network approval | Verify Coolify vars: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `BOOKING_SUPABASE_URL`, `BOOKING_SUPABASE_SERVICE_ROLE_KEY`, `CRM_INGEST_API_KEY`, `CRM_BOOKING_SOURCE_ID` |
| Production build must pass | Build compiles current Booking UI, Website Reservations, Bulk Import, Audit Logs, Settings structure, and prior performance/drawer changes | `app/layout.tsx`, Next build | N/A | Passed | `npm run build` | Passed on 2026-07-10; Next build also performed lint/type validation | Consider self-hosting font to avoid build-time Google dependency recurring in uncached environments |
| Available tests must pass | Previous phase full suite passed 178/178; current phase test attempt is blocked by environment, not by a test assertion failure | `lib/**/*.test.ts` | N/A | Blocked in current run | `npm run test` attempted on 2026-07-10 | Sandbox run failed with `listen EPERM` on tsx temp IPC pipe; escalated rerun was rejected by usage-limit policy, so no current result | Re-run `npm run test` when escalation/usage budget is available; pure-logic + nav coverage only, DB-bound persistence still needs live/integration tests |
| Lint must run | `npm run lint` invokes deprecated/interactive `next lint` setup prompt | `package.json` | N/A | Blocked | `npm run lint` | Still prompts for ESLint config and exits; non-interactive lint script unavailable | Prompt 3: add non-interactive ESLint config/script or remove broken lint script |

## Final Adversarial Verification — 2026-07-11

Status meanings are literal: PASS includes applicable persistence/runtime evidence;
PARTIAL means the implementation exists but an acceptance dimension is unproved;
BLOCKED identifies the exact unavailable external condition.

| Requirement | Status | Evidence / files / database objects | Test performed and result | Remaining issue |
|---|---|---|---|---|
| Prototype-behavior search | PARTIAL | Repository-wide search reviewed TODO/FIXME/mock/demo/fake/placeholder/Phase wording, empty handlers, console-only actions, local storage, disabled controls and catches. Stale README/integration claims removed. Explicit baseline fixtures are addressed by `0015_remove_production_seed_leads.sql`. | `rg` audit; live query found exactly 9 `metadata.seed=true` leads. | Migration 0015 must run in deployment; test fixtures remain live until then. |
| 20,000+ lead list architecture | PASS | `getLeadsPage()` uses DB filters/sort/search plus `range`; New, Qualified and Database use page size 30. Follow-Up/Post-Op now return `FollowUpListResult` with range/count pagination. `lib/data/supabase.ts`, list pages. | Live 27-row DB: paged list median 119.9 ms, server search median 119.8 ms. Code inspection confirms no full browser download. | No production-sized 20,000-row DB was available; no 20k latency claim. |
| Drawer/query architecture | PARTIAL | Lazy `/api/leads/[id]/tab`, client tab cache, compact lead existence lookup, SQL channel filter, latest 100 messages/comments, 200 timeline events, batched duplicate members. | Read-only shell-row median 122.0 ms; latest-message median 107.8 ms. | “Load older” controls are not implemented; authenticated production drawer test was blocked by expired session. |
| Pipeline counters and stage exclusivity | PASS | Pipeline counts use six DB count queries; stage pages lock `new`, `qualified+booked`, `follow_up`, `post_op_follow_up`; Database is all. | Live counts: new 19, qualified 2, booked 2, follow-up 1, post-op 2, lost 1. One primary status column prevents conflicting primary stages. | Booking-linked but still-awaiting-confirmation leads remain New by design until confirmed/booked. |
| Status confirmation and persistence | PARTIAL | `LeadDetail.tsx` opens old/new confirmation before `updateLeadStageAction`; mutation writes status, status history, audit and timeline, then refreshes lists. | Code/schema/type/lint/unit verification. | Live New→Qualified browser persistence/audit test blocked by expired authenticated session. |
| Tag definitions/assignment permissions | PARTIAL | Admin/Auditor definition mutations require settings capability; lead assignment validates active IDs; viewer lead writes now denied by `leads.edit`; Moderator cannot manage global settings. | Permission tests plus code inspection. | Live Admin/Auditor/Moderator assignment and refresh smoke blocked by auth. |
| Notes persistence | PARTIAL | Three note keys map to `leads.notes`, `medical_history`, `medical_notes`; update requires returned row and writes audit/timeline. | Code/schema/type/lint verification. | Save→tab→close→refresh browser test blocked by auth. |
| Messenger / Instagram | PARTIAL | Messenger label and FB/IG-only SQL filtering; canonical `crm_messages`, attachments and reactions; latest 100 bounded. | 44 Meta ingestion scenarios pass in automated tests. | Live rendered thread and incremental older-page navigation not tested. |
| WhatsApp readiness | PARTIAL | Server-only config, authenticated normalized ingest endpoint, canonical message/attachment storage, honest unconfigured state, no fake connection. | Route/build/security inspection. | No credentials or live webhook/outbound provider test; outbound send is not implemented. |
| Follow-up plans | PARTIAL | Arbitrary ordered/versioned settings and snapshotted lead steps; done/snooze/lost server mutations; Follow-Up links include `tab=Follow-Up`; queues paginated. | Code/schema/unit/build checks. | Live 3-step/10-step creation and state transition smoke blocked by auth. |
| Live booking / one scheduling source | PARTIAL | CRM reads Admin booking doctors/services/schedules/blocks/appointments; no CRM availability copy. `lib/booking/service.ts`, Booking tab, Calendar, Reservations. Live catalog has 23 doctors, 95 services, 9 appointments. | Read-only cross-project counts and schema checks passed. | Authenticated drawer slot/create flow and public-site slot parity were not executable; production session expired. |
| Website Reservations | PARTIAL | Correct route/name, live booking appointments, lead link/sync, status history, Calendar and Log updates. | Live booking appointment count 9; code/schema/build checks. | Creating a new test reservation would pollute production and authenticated UI was unavailable. |
| Bulk Import | PARTIAL | CSV/TSV and XLSX parser, remapping, preview, validation, confirmation, canonical lead/finance writes and audit. | Import mapping/delimited tests pass; code/build inspection. | Authenticated real CSV/XLSX file chooser and persistence/rollback smoke not run. |
| Audit Logs | PARTIAL | One `audit_logs` sink; Admin/Auditor route guard; user/role/action/entity/lead/date/old/new filters. Live table has 62 rows. | Read-only count, permission matrix and build passed. | Fresh mutation event coverage and Moderator 403 browser smoke blocked by auth. |
| CRM Settings | PARTIAL | Requested sections exist; real editors for tags, lost/escalation reasons, SLA/follow-up, reports, AI prompt, email and scheduling; read-only rules are disclosed. | Schema tables present; lint/type/tests/build. | Some structural sections remain read-only rather than fully configurable; live save/refresh smoke blocked. |
| Financial source of truth/settings | PARTIAL | Canonical finance tables reused; `/financial/settings` reads all 95 booking services and 23 Admin doctors; service prices, discount precedence, consumables/defaults, doctor comp, external defaults and dashboard/drilldowns use canonical tables. | Financial unit tests pass; live schema migration 0014 columns/tables present. | Live `crm_lead_financials` count is 0, so quote/payment/refund persistence and dashboard totals cannot be certified. |
| Financial permissions | PASS | Server capability checks: Moderator quote/payment/escalation only; Admin/Auditor rules, doctor-funded payment, consumables/external costs/compensation; sensitive sections hidden and server-denied. | Permission tests include `leads.edit`; financial action inspection. | Action-level DB integration tests would add defense beyond pure capability tests. |
| Admin doctor source of truth | PARTIAL | Financial dropdown reads booking/Admin `doctors`; CRM create inserts into that table with `is_active=false`; public catalog filters `is_active=true`. Fake lead-list doctor filter replaced by canonical catalog. | Live read-only catalog: 23 doctors, active/inactive states observed. | Creating a doctor was not run because it is a persistent Admin-side change with no cleanup API; visibility/default behavior is code-verified only. |
| Theme and language | PARTIAL | SSR cookies set `lang`, `dir`, `data-theme`; semantic light/dark tokens; English/Arabic nav dictionaries. | Code/build/CSS inspection. | Deep functional pages remain substantially English; authenticated visual matrix was blocked. |
| Security/secrets | PASS | Secrets occur only in server modules/env example; no service-role/Meta/WhatsApp/AI/Resend/cron value is browser-exposed. Raw tab/WhatsApp/server-action DB errors now stay server-side. `CRM_DATA_SOURCE=mock` rejected in production. | Secret-name scan, client/server import inspection, `npm audit --omit=dev` = 0 vulnerabilities after Next 15.5.20/PostCSS patch. | Deployment environment and built artifacts still require operational secret rotation policy outside this repo. |
| Lint/type/tests/build | PASS | `.eslintrc.json`, package scripts/dependencies, source fixes. | `npm run lint` pass; `npx tsc --noEmit` pass; `npm run test` pass (179 after new permission test); `npm run build` pass. | DB-bound acceptance tests remain separate from pure automated suite. |
| Critical production errors | PARTIAL | Status/tag errors: eager full-drawer/broad revalidation and escaped errors; Notes: update did not prove a row changed; Payment: non-function export in `use server` module plus broad refresh; Booking: non-Booking errors escaped boundary. Boundaries and lazy architecture corrected. | Source history/current code inspection; build/tests. | Exact prior production digests/log stacks were unavailable and authenticated retest was blocked, so these are not marked live PASS. |
