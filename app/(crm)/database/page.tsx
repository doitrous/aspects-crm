import { ComingSoon } from "@/components/ui/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Database Leads"
      phase="Phase 2"
      summary="Unified searchable database of all leads — new and historical — with full stage, follow-up, booking, notes, conversation, escalation and merge history."
    />
  );
}
